# Sigil API — Environment Variables
# Copy this file to .env and fill in your values.
# All variables are prefixed with SIGIL_ (handled by pydantic-settings).

# --- Application ---
SIGIL_DEBUG=false
SIGIL_LOG_LEVEL=INFO

# --- Server ---
SIGIL_HOST=0.0.0.0
SIGIL_PORT=8000

# --- CORS ---
# Comma-separated list of allowed origins
SIGIL_CORS_ORIGINS=["https://app.sigilsec.ai","http://localhost:3000"]

# --- JWT / Auth ---
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SIGIL_JWT_SECRET=changeme-generate-a-real-secret
SIGIL_JWT_ALGORITHM=HS256
SIGIL_JWT_EXPIRE_MINUTES=60

# --- Database (choose one) ---
# Option A: Supabase (recommended)
# Get credentials from: https://supabase.com/dashboard/project/<project-id>/settings/api
SIGIL_SUPABASE_URL=https://<project>.supabase.co
SIGIL_SUPABASE_KEY=<service-role-key>
SIGIL_SUPABASE_PUB_KEY=<publishable-key>

# Option B: Direct Postgres connection string
# Format: postgresql://user:password@host:port/database
SIGIL_DATABASE_URL=postgresql://postgres:password@db.<project>.supabase.co:5432/postgres

# --- Redis (optional — for caching / rate limiting) ---
SIGIL_REDIS_URL=redis://localhost:6379

# --- Threat Intel ---
SIGIL_THREAT_INTEL_TTL=3600

# --- SMTP (optional — for email alert notifications) ---
SIGIL_SMTP_HOST=smtp.example.com
SIGIL_SMTP_PORT=587
SIGIL_SMTP_USER=alerts@sigilsec.ai
SIGIL_SMTP_PASSWORD=<smtp-password>
SIGIL_SMTP_FROM_EMAIL=alerts@sigil.dev

# --- Stripe (billing) ---
# Get these from https://dashboard.stripe.com
SIGIL_STRIPE_SECRET_KEY=sk_live_...
SIGIL_STRIPE_WEBHOOK_SECRET=whsec_...
SIGIL_STRIPE_PRICE_PRO=price_...
SIGIL_STRIPE_PRICE_TEAM=price_...
SIGIL_STRIPE_PRICE_PRO_ANNUAL=price_1T2AnKFhPhxEz27fNp6Kt7O3
SIGIL_STRIPE_PRICE_TEAM_ANNUAL=price_1T2AlpFhPhxEz27f8tni554h

# Evidence: US-101 — Anthropic-first LLM config with current model registry

**Date:** 2026-06-11 · **Feature:** F-009 · **Branch:** feature/sigil-pro-fable

## Change
`api/llm_config.py`:
- `provider` default `openai` → `anthropic`
- `model` default `gpt-4-turbo` → `claude-opus-4-8`
- New `deep_model` (env `LLM_DEEP_MODEL`, default `claude-fable-5`) and `fast_model` (env `LLM_FAST_MODEL`, default `claude-haiku-4-5`)
- `api_key`: `LLM_API_KEY` → fallback to `ANTHROPIC_API_KEY` (anthropic provider only)

## Verification (run fresh 2026-06-11)
TDD Red: 10 failed / 5 passed (failures = wrong defaults + missing attrs — right reason).

TDD Green:
```
$ python3 -m pytest api/tests/test_llm_config.py -q
15 passed in 0.10s
```

Regression (full suite):
```
$ python3 -m pytest api/tests -q
239 passed, 341 skipped, 6 warnings in 2.27s
```
Exit 0, no failures. Baseline before story: 223 passed / 339-341 skipped — delta is the new tests; zero existing tests broken.

## Notes
- Pyright pre-existing diagnostics on `provider` Literal assignment and `get_headers` return type predate this change (same code shape before/after); out of story scope.
- `temperature` remains in config — it is stripped from Fable/Opus 4.8 payloads in US-103, not here.

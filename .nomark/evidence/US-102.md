# Evidence: US-102 — Model router registry refresh (retire claude-3 tiers)

**Date:** 2026-06-11 · **Feature:** F-009 · **Branch:** feature/sigil-pro-fable

## Change
- `api/services/model_router.py`: MODELS re-keyed to exact current aliases — `claude-haiku-4-5` (×1), `claude-opus-4-8` (×5), `claude-fable-5` (×10) per verified $1/$5 · $5/$25 · $10/$50 MTok pricing; `api_name == key` (no date suffixes); downgrade preference and error fallback now read `llm_config` instead of hardcoded retired IDs.
- `api/utils/complexity_scorer.py` (**scope expansion, owner-approved this session**): tier→model mapping is llm_config-driven (SIMPLE→fast_model, MODERATE→model, COMPLEX→deep_model); cost tables re-keyed by tier (`fast`/`standard`/`deep`); savings baseline = deep model.
- `api/tests/test_model_routing.py` (pre-existing test file): retired-ID assertions mechanically updated to config-driven expectations (owner pre-authorized mechanical swaps).

## Verification (run fresh 2026-06-11)
TDD Red: 12/12 new tests failed on old registry (right reason).

Green:
```
$ python3 -m pytest api/tests/test_model_routing.py api/tests/test_model_router.py -q
17 passed in 0.16s
$ python3 -m pytest api/tests -q
251 passed, 341 skipped, 6 warnings in 2.23s
$ grep -rn "claude-3" api/services/model_router.py api/utils/complexity_scorer.py api/tests/test_model_routing.py | wc -l
0
```

## Notes
- Mid-story regression: 4 tests in `test_model_routing.py` failed against the new registry — stale ID assertions, not behavior bugs; updated as above.
- Pyright diagnostics in `model_router.py` (MssqlClient.fetch_all/execute, utcnow deprecation) are pre-existing in untouched functions.

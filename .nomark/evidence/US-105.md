# Evidence: US-105 — require_llm_access gate (402 on exhausted free tier)

**Date:** 2026-06-11 · **Feature:** F-009 · **Branch:** feature/sigil-pro-fable
**Authorization change owner-approved this session (CHARTER II.5) before the edit.**

## Change (`api/gates.py` — additive only)
`require_llm_access(credits_required=1)` dependency factory, house style (mirrors `require_plan`):
- 401 unauthenticated via `get_current_user_unified` (wiring asserted by test)
- PRO/TEAM/ENTERPRISE: always pass — metered by US-104, never blocked (owner's fair-use decision)
- FREE: passes while `credit_service.check_llm_allowance` allows
- Exhausted FREE: HTTP 402 with `{detail, reason, balance, credits_required, reset_date, upgrade_url}`
No existing gate modified.

## Verification (run fresh 2026-06-11)
New cases (file is in the extended suite — requires `SIGIL_RUN_EXTENDED_TESTS=1`):
```
$ SIGIL_RUN_EXTENDED_TESTS=1 python3 -m pytest api/tests/test_tier_gating.py -q -k RequireLlmAccess
5 passed, 28 deselected in 0.23s
$ python3 -m pytest api/tests -q
267 passed, 346 skipped in 1.98s        # default suite — zero regressions
```

## ⚠️ AC deviation — documented, not hidden
The story's "done when" said the whole `test_tier_gating.py` file exits 0. That is **not achievable without out-of-scope repairs**: at HEAD (verified via `git stash` comparison) the file already fails 8/28 tests (`TestTierGatingAPIEndpoints`, `TestTierGatingPerformance`, `TestTierGatingEdgeCases`) — pre-existing rot in the skip-by-default extended suite. Those failures also wedge the **session-scoped event loop** (`conftest.py:151`, a deprecated pytest-asyncio pattern), which then breaks any async test running after them in file order — including the new ones, though they pass in isolation and as a class.

Evidence of pre-existence:
```
$ git stash push api/gates.py api/tests/test_tier_gating.py && SIGIL_RUN_EXTENDED_TESTS=1 pytest test_tier_gating.py -q
8 failed, 20 passed          # at HEAD, before any US-105 change
```

**Follow-up logged (not blocking):** repair the 8 pre-existing extended-suite failures + migrate conftest off the session-scoped `event_loop` fixture (deprecation warning already fires).

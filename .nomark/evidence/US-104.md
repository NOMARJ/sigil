# Evidence: US-104 — LLM usage metering (free teaser + Pro fair-use)

**Date:** 2026-06-11 · **Feature:** F-009 · **Branch:** feature/sigil-pro-fable

## Change (`api/services/credit_service.py`)
- `CREDIT_RATES` refreshed: exactly `claude-haiku-4-5` (1), `claude-opus-4-8` (5), `claude-fable-5` (10) credits/1K tokens — proportional to verified pricing; retired claude-3 and all gpt-* rates removed.
- `monthly_allowance(tier)`: FREE allowance env-overridable via `LLM_FREE_MONTHLY_CREDITS` (default 50 credits ≈ 10 typical 4-credit analysis calls — the PRD's "10 calls/month" expressed in the existing credit currency); other tiers unchanged. `initialize_user_credits` now uses it.
- `check_llm_allowance(user_id, credits_required)`: structured decision — `{allowed, balance}` or `{allowed: False, reason: "allowance_exhausted", balance, credits_required, reset_date, upgrade_url}`. Never raises on exhaustion (callers convert to 402).
- `record_llm_usage(user_id, model, input_tokens, output_tokens, feature, ...)`: meters via existing `deduct_credits` → `sp_DeductCredits` (existing table/procedure — **no schema change**, CHARTER II.5 constraint held); feature→TransactionType mapping with "scan" fallback; token split preserved in metadata.
- `UPGRADE_URL` env-overridable (`SIGIL_UPGRADE_URL`), default `https://www.sigilsec.ai/pricing` (URL verified live in LAUNCH-001 evidence).

## Verification (run fresh 2026-06-11)
TDD Red: collection ImportError on `monthly_allowance` (right reason).

Green:
```
$ python3 -m pytest api/tests/test_llm_metering.py -q
11 passed in 0.14s
$ python3 -m pytest api/tests -q
267 passed, 341 skipped, 6 warnings in 2.29s
```

## Notes
- Discovery: existing credit infra (user_credits, credit_transactions, sp_DeductCredits, FREE=50/mo allocation) already carried the teaser structure — story adds the two LLM-facing functions and the rates refresh rather than new machinery.
- DB calls mocked at service-method level in tests; the deduct path itself is the long-standing stored procedure.
